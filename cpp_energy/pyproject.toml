[build-system]
requires = ["scikit-build-core>=0.9.8", "pybind11>=2.12.0"]
build-backend = "scikit_build_core.build"

[project]
name = "cpp_energy"
version = "0.1.0"
requires-python = ">=3.10"

[tool.scikit-build]
cmake.version = ">=3.18"
build.verbose = true
